# Checkpoint de Contexto - Correção de Configurações de IA

**Data:** 2025-12-07 12:12:41
**Etapa concluída:** Batch B - Resume Generator Integration
**Próximas etapas:** Batch C (UI Updates), Batch D (Cleanup), Batch E (Testes), Verificação Final
**Tokens restantes no momento do checkpoint:** ~110,736 / 200,000 (55.4%)

---

## 📊 STATUS ATUAL

### ✅ O que foi feito até agora

**Batch A - Job Parser Integration (✅ COMPLETO)**

O Job Parser agora carrega e usa as configurações salvas em `prompts_config` (Supabase) em vez de valores hardcoded.

**Arquivos modificados:**

1. **`lib/ai/analysis-prompts.ts`**
   - Removido import de `UserProfile`
   - Mudou assinatura: `buildJobAnalysisPrompt(jobDescription, userProfile: UserProfile)` → `buildJobAnalysisPrompt(jobDescription, dossiePrompt: string)`
   - Agora insere `dossiePrompt` diretamente no prompt (não mais interpola campos individuais de `userProfile`)

2. **`lib/ai/job-parser.ts`**
   - Adicionado imports: `loadUserAIConfig`, `getGenerationConfig`
   - Removido import: `USER_PROFILE`
   - Função `parseJobWithAnalysis()`:
     - Novo parâmetro: `userId?: string`
     - Chama `loadUserAIConfig(userId)` no início
     - Cria modelo usando `config.modelo_gemini`, `getGenerationConfig(config)`, `ANALYSIS_SYSTEM_PROMPT`
     - Usa `config.dossie_prompt` em vez de `USER_PROFILE` hardcoded
     - Retorna `model: config.modelo_gemini`

3. **`app/api/ai/parse-job/route.ts`**
   - Adicionado import: `createClient` de `@/lib/supabase/server`
   - Obtém `userId` da sessão via `supabase.auth.getUser()`
   - Passa `userId` para `parseJobWithAnalysis(jobDescription, userId)`

**Arquivos de teste atualizados:**

1. **`__tests__/lib/ai/job-parser.test.ts`**
   - Mock de `@/lib/ai/config` agora exporta:
     - `createGeminiClient` (retorna mock de `getGenerativeModel`)
     - `loadUserAIConfig` (retorna config fake com Grok)
     - `getGenerationConfig`
     - `ANALYSIS_SYSTEM_PROMPT`
   - Removido mock de `@/lib/ai/user-profile`
   - Atualizado modelo esperado: `x-ai/grok-4.1-fast`

2. **`__tests__/app/api/ai/parse-job/route.test.ts`**
   - Adicionado mock de `@/lib/supabase/server` com `createClient` retornando usuário fake
   - Atualizado modelo esperado em todos os testes: `x-ai/grok-4.1-fast`

3. **`__tests__/lib/ai/analysis-prompts.test.ts`**
   - Removido import de `USER_PROFILE`
   - Criado `dossiePrompt` fake com perfil de Engenharia Química
   - Todos os testes agora usam `dossiePrompt` em vez de `USER_PROFILE`

**Comandos executados com sucesso:**

```bash
pnpm lint                    # ✅ Passou (apenas warnings pré-existentes)
pnpm test -- --run          # ✅ 216/218 passando
```

**Resultados dos testes:**

- Unit: 216 passando
- 2 falhas pré-existentes em `config.test.ts` (validação de API key - não relacionado aos batches)

---

**Batch B - Resume Generator Integration (✅ COMPLETO)**

O Resume Generator agora carrega e usa as configurações salvas em `prompts_config` para personalização de currículos.

**Arquivos modificados:**

1. **`lib/ai/resume-generator.ts`**
   - Adicionado imports: `loadUserAIConfig`, `getGenerationConfig`
   - Removido imports: `GEMINI_CONFIG`, `RESUME_SYSTEM_PROMPT`
   - Função `generateTailoredResume()`:
     - Novo parâmetro: `userId?: string`
     - Chama `loadUserAIConfig(userId)` no início
     - Usa `config.curriculo_prompt` para system instruction (em vez de `RESUME_SYSTEM_PROMPT`)
     - Usa `getGenerationConfig(config)` para parâmetros do modelo
     - Retorna `config.modelo_gemini` como modelo usado

2. **`app/api/ai/generate-resume/route.ts`**
   - Obtém `userId` da sessão via `supabase.auth.getUser()`
   - Passa `userId` para `generateTailoredResume(jobDetails, language, userId)`

**Arquivos de teste atualizados:**

1. **`__tests__/lib/ai/resume-generator.test.ts`**
   - Mock de `@/lib/ai/config` agora exporta:
     - `createGeminiClient`
     - `loadUserAIConfig` (retorna config fake com Grok)
     - `getGenerationConfig`
   - Removido `RESUME_SYSTEM_PROMPT` do mock de `resume-prompts`
   - Atualizado modelo esperado: `x-ai/grok-4.1-fast`

2. **`__tests__/app/api/ai/generate-resume/route.test.ts`**
   - Adicionado `auth.getUser()` ao mock de `@/lib/supabase/server`
   - Removido mock de `GEMINI_CONFIG`
   - Atualizado modelo esperado: `x-ai/grok-4.1-fast`

**Comandos executados com sucesso:**

```bash
pnpm lint                          # ✅ Passou
pnpm test -- resume-generator --run  # ✅ Passou
```

---

## 🎯 PRÓXIMOS PASSOS (Continuação)

### Batch C: UI Updates

**Objetivo:**
Atualizar labels/placeholders na UI de configurações para refletir modelo correto (Grok via OpenRouter).

**Arquivos a modificar:**

1. **`components/configuracoes-prompts.tsx`**
   - Linha ~207: Trocar "Modelo Gemini" → "Modelo LLM"
   - Linha ~212: Atualizar placeholder `gemini-2.5-flash` → `x-ai/grok-4.1-fast`
   - Linha ~215: Atualizar help text para mencionar Grok/OpenRouter
   - Linha ~160: Remover menção a "Gemini" na descrição do card

**Comandos a executar:**

```bash
pnpm lint
```

**Critério de sucesso:**

- ✅ UI mostra "Modelo LLM" (não "Modelo Gemini")
- ✅ Placeholder correto (`x-ai/grok-4.1-fast`)
- ✅ Descrição menciona OpenRouter/Grok

---

### Batch D: Cleanup

**Objetivo:**
Remover código hardcoded deprecado.

**Arquivos para remover/deprecar:**

1. **`lib/ai/user-profile.ts`** - REMOVER (substituído por `config.dossie_prompt`)
2. Verificar e remover imports de `USER_PROFILE` em outros arquivos

**Comandos a executar:**

```bash
rm lib/ai/user-profile.ts
grep -r "USER_PROFILE" lib/ app/ components/  # Verificar se há referências restantes
pnpm lint
pnpm test -- --run
```

**Critério de sucesso:**

- ✅ `user-profile.ts` removido
- ✅ Nenhuma referência a `USER_PROFILE` no código (exceto testes antigos a serem removidos)
- ✅ Todos os testes ainda passam

---

### Batch E: Testes

**Objetivo:**
Garantir cobertura de testes para uso de configs personalizadas.

**Testes a criar/atualizar:**

1. **Unit Test:** `__tests__/lib/ai/config.test.ts`
   - Adicionar teste para `loadUserAIConfig(userId)` retornando config correta
   - Adicionar teste para `getGenerationConfig(config)` extraindo campos certos

2. **Integration Test (se necessário):**
   - Validar que análise de vaga menciona dados do dossiê customizado

3. **E2E Test (Playwright):** `tests/e2e/ia-settings.spec.ts`
   - Acessar Configurações > Prompts de IA
   - Atualizar dossiê (mudar formação, localização)
   - Salvar
   - Ir para análise de vaga
   - Verificar que texto da análise reflete novo dossiê

**Comandos a executar:**

```bash
pnpm test -- config.test.ts --run
pnpm test:e2e -- ia-settings
```

**Critério de sucesso:**

- ✅ Testes unitários cobrem `loadUserAIConfig` e `getGenerationConfig`
- ✅ E2E confirma que dossiê atualizado reflete na análise

---

### Verificação Final

**Comandos:**

```bash
pnpm lint
pnpm test -- --run
pnpm test:e2e
```

**Critério de sucesso:**

- ✅ Lint passa
- ✅ Todos os unit tests passam
- ✅ E2E tests passam
- ✅ Análise de vaga **NÃO menciona mais** "engenheiro de computação"
- ✅ Análise de vaga **USA** perfil salvo (Engenharia Química, Bertioga/SP)
- ✅ UI mostra modelo correto (Grok)
- ✅ Parâmetros salvos (temperatura, tokens) são aplicados nas chamadas de IA

---

## 🔍 CONTEXTO TÉCNICO IMPORTANTE

**Plano de diagnóstico:**

- Documento completo em `docs/ia/ia-settings-refactor.md`
- Root cause identificado: Rotas de IA ignoram `prompts_config` e usam valores hardcoded

**Dependências chave:**

- `@/lib/ai/config.ts` - Funções `loadUserAIConfig()`, `getGenerationConfig()`
- `@/lib/supabase/prompts.ts` - CRUD de `prompts_config`
- Tabela Supabase: `prompts_config`

**Convenções do projeto:**

- Imports com `@/*` para root-level
- Server Components: async, usa `createClient` de `@/lib/supabase/server`
- Client Components: sync, usa `createClient` de `@/lib/supabase/client`

**Armadilhas conhecidas:**

- Testes unitários precisam mockar `loadUserAIConfig` e `getGenerationConfig`
- Rotas de API precisam obter `userId` via `supabase.auth.getUser()`
- Modelo usado agora é `x-ai/grok-4.1-fast` (não mais Gemini)

**Arquivos modificados até agora:**

```
M CLAUDE.md
M __tests__/app/api/ai/generate-resume/route.test.ts
M __tests__/app/api/ai/parse-job/route.test.ts
M __tests__/lib/ai/analysis-prompts.test.ts
M __tests__/lib/ai/job-parser.test.ts
M __tests__/lib/ai/resume-generator.test.ts
M app/api/ai/generate-resume/route.ts
M app/api/ai/parse-job/route.ts
M lib/ai/analysis-prompts.ts
M lib/ai/job-parser.ts
M lib/ai/resume-generator.ts
```

---

## 📋 CHECKLIST DE RETOMADA

Ao retomar após `clear`:

1. [ ] Ler este checkpoint completo
2. [ ] Verificar estado atual dos arquivos modificados: `git status`
3. [ ] Rodar testes para confirmar estado atual: `pnpm test -- --run`
4. [ ] Continuar na Etapa: **Batch C - UI Updates**

---

**Comando para retomar:**

```bash
cat docs/sessions/checkpoint-20251207-121241.md
git status
pnpm test -- --run
```

---

**Progresso geral:**

- ✅ Batch A: Job Parser Integration (100%)
- ✅ Batch B: Resume Generator Integration (100%)
- ⏳ Batch C: UI Updates (0%)
- ⏳ Batch D: Cleanup (0%)
- ⏳ Batch E: Testes (0%)
- ⏳ Verificação Final (0%)

**Total concluído:** ~33.3% (2/6 batches)
